import React from 'react';
import { convertObjectToString } from '../utils/common';
const Camera = React.forwardRef(({ 'look-controls': lookControls, active, ...props }, ref) => React.createElement('a-camera', {
    ...convertObjectToString(props),
    'look-controls': `enabled: ${lookControls ?? true}`,
    active: active ?? true,
    ref,
}));
Camera.displayName = 'Camera';
export default Camera;
