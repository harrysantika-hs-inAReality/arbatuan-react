import React from 'react';
const Assets = React.forwardRef(({ children, timeout }, ref) => React.createElement('a-assets', { timeout, ref }, children));
Assets.displayName = 'Assets';
export default Assets;
